#include "main.h"
/**
 *  * mul -multiply two integer and return the result
 *   * @a: first number
 *    * @b: second number
 *     *
 *      * Description: multiply two integer and return the result
 *       *
 *        * Return:  result of a*b
 */
int mul(int a, int b)
{
	return (a * b);
}
